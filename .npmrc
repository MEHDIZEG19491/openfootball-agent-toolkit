# Next's bundled lint plugins have ESLint 9 peer ranges. Their removed accessors
# are adapted in scripts/eslint-compat.mjs and all lint rules remain active.
# Retire this setting together with the adapter when upstream supports ESLint 10.
legacy-peer-deps=true
